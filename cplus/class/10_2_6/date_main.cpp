/*
	test a method with const
	const is apart of method
	if define a const method, const must be appended
*/
#include <stdio.h>
#include "date.h"
int main()
{
	Date date;
	printf("date.year = %d\n", date.year());
}
